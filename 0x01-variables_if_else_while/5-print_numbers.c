#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Prints all numbers to base 10 form 0'
 * Return: Always zero
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");

	return (0);
}
